{% extends 'base.html.twig' %}
{% block title %}Modifier mes informations | E-Motion{% endblock %}

{% block body %}
    <a href="{{path('app_user_space')}}">Retour menu</a>
    <h1>Modification informations utilisateur</h1>
<div>
    <h1>Espace de connexion</h1>
     {{ form_start(UpdateInformation) }}
        {{ form_row(UpdateInformation.nom) }}
        {{ form_row(UpdateInformation.prenom) }}
        {{ form_row(UpdateInformation.email) }}
        {{ form_row(UpdateInformation.adresse) }}
        {{ form_row(UpdateInformation.telephone) }}
    <a style="color:#2563EB" href="{{ path('app_politique_confidentialite') }}">En validant mon opération, je clame que les informations me concernant sont exactes</a>
    <input type="checkbox" name="valid_term" required/>
        <br>
    <button style="background-color: cyan;" type="submit" class="btn">Valider</button>
    <button>
        <a href="{{ path('app_user_space') }}">Annuler</a>
    </button>
    {{ form_end(UpdateInformation) }}
</div>
{% endblock %}
