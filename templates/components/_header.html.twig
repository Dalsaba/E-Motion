<header class=" bg-blue-600 text-white h-32 flex flex-col items-center justify-center">
{#    <div class=" w-4/5 flex justify-between">#}
{#        <h1 class="font-bold text-2xl w-2/5 "><a href="{{ path('app_default') }}" class="text-white">EMOTION</a></h1>#}
{#        <nav class="w-2/5 grid justify-items-end">#}
{#            <ul class="flex gap-x-6">#}
{#                {% if app.user %}  #}{# Si un client est connecté #}
{#                    <li><a href="" class="text-white">Espace client</a></li>#}
{#                    <li><a href="{{ path('app_panier') }}" class="text-white">Panier</a></li>#}
{#                    <li><a href="{{ path('app_logout') }}" class="text-white">Déconnexion</a></li>#}
{#                {% else %} #}{# Si un client n'est pas connecté #}
{#                    <li><a href="{{ path('app_connexion_client') }}" class="text-white">Connexion</a></li>#}
{#                {% endif %}#}
{#            </ul>#}
{#        </nav>#}

        <nav id="header" class=" bg-blue-600 fixed w-full z-30 top-0 text-white">
            <div class="w-full container mx-auto flex flex-wrap items-center justify-between mt-0 py-2">
                <div class="pl-4 flex items-center">
                    <a class="toggleColour text-white no-underline hover:no-underline font-bold text-2xl lg:text-4xl" href="#">
                        <!--Icon from: http://www.potlabicons.com/ -->
{#                        <svg class="h-8 fill-current inline" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512.005 512.005">#}
{#                            <rect fill="#2a2a31" x="16.539" y="425.626" width="479.767" height="50.502" transform="matrix(1,0,0,1,0,0)" />#}
{#                            <path#}
{#                                    class="plane-take-off"#}
{#                                    d=" M 510.7 189.151 C 505.271 168.95 484.565 156.956 464.365 162.385 L 330.156 198.367 L 155.924 35.878 L 107.19 49.008 L 211.729 230.183 L 86.232 263.767 L 36.614 224.754 L 0 234.603 L 45.957 314.27 L 65.274 347.727 L 105.802 336.869 L 240.011 300.886 L 349.726 271.469 L 483.935 235.486 C 504.134 230.057 516.129 209.352 510.7 189.151 Z "#}
{#                            />#}
{#                        </svg>#}
                        EMOTION
                    </a>
                </div>
                <div class="w-full flex-grow lg:flex lg:items-center lg:w-auto hidden mt-2 lg:mt-0 bg-white lg:bg-transparent text-black p-4 lg:p-0 z-20" id="nav-content">
                    <ul class="list-reset lg:flex justify-end flex-1 items-center">
                        {% if app.user %}
                        <li class="mr-3">
                            <a class="inline-block py-2 px-4 text-black font-bold no-underline" href="">Espace client</a>
                        </li>
                        <li class="mr-3">
                            <a class="inline-block text-black no-underline hover:text-gray-800 hover:text-underline py-2 px-4"
                               href="{{ path('app_panier') }}">Panier</a>
                        </li>
                        <li class="mr-3">
                            <a class="inline-block text-black no-underline hover:text-gray-800 hover:text-underline py-2 px-4"
                               href="{{ path('app_logout') }}">Déconnexion</a>
                        </li>
                    </ul>
                    {% else %}
                    <button
                            id="navAction"
                            class="mx-auto lg:mx-0 hover:underline bg-white text-gray-800 font-bold rounded-full mt-4 lg:mt-0 py-4 px-8 shadow opacity-75 focus:outline-none focus:shadow-outline transform transition hover:scale-105 duration-300 ease-in-out"
                            onclick="location.href='/connexion_client'"
                    >
                        Connexion
                    </button>
{#                        <form action="{{ path('app_connexion_client') }}" method="post">#}
{#                            <button    id="navAction"#}
{#                                       class="mx-auto lg:mx-0 hover:underline bg-white text-gray-800 font-bold rounded-full mt-4 lg:mt-0 py-4 px-8 shadow opacity-75 focus:outline-none focus:shadow-outline transform transition hover:scale-105 duration-300 ease-in-out"#}
{#                                       type="submit"> Connexion </button>#}
{#                        </form>#}
                    {% endif %}
                </div>
            </div>
        </nav>

    </div>

</header>

<div class="flex items-center justify-center bg-gradient-to-r from-cyan-500 to-blue-500">
    {% for type, messages in app.flashes %}
        {% for m in messages %}
            <p class="py-3 px-5 border border-solid border-transparent rounded {{ type }}">{{ m }}</p>
        {% endfor %}
    {% endfor %}
</div>