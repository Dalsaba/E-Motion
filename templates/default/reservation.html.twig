{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-6xl">Faites votre reservation</h1>
    <p> {{ vehicule.marque }}</p>
{#        {{ form(ajout) }}#}

    {{ form_start(ajout) }}
    <div class=" mt-6">
        {{ form_row(ajout.DateDeDebut, {}) }}
    </div>
    <div class=" mt-6">

        {{ form_row(ajout.DateDeFin, {}) }}
    </div>
    <div class=" mt-6 w-2/6 h-1/6 bg-cyan-500 hover:bg-cyan-600 rounded-xl flex justify-center grid content-center text-white">
        {{ form_row(ajout.Sauvegarder, {}) }}
    </div>
    {{ form_end(ajout) }}



{% endblock %}
